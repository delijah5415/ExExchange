<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="CryptoExchange — secure digital asset exchange platform"><title>{{ $title ?? config('app.name') }}</title><link rel="icon" href="{{ asset('images/logo/logo.svg') }}"><link rel="stylesheet" href="{{ asset('css/site.css') }}"></head><body>
<header class="nav"><a class="brand" href="{{ route('home') }}"><span class="brand-mark">CX</span><span>Crypto<span>Exchange</span></span></a><nav><a href="{{ route('exchange') }}">Exchange</a><a href="{{ route('faq') }}">FAQ</a><a href="{{ route('contacts') }}">Support</a>@if(session('supabase_token'))<a class="pill" href="{{ route('account') }}">Account</a><form method="POST" action="{{ route('logout') }}" class="inline">@csrf<button>Sign out</button></form>@else<a href="{{ route('login') }}">Sign in</a><a class="pill" href="{{ route('register') }}">Create account</a>@endif</nav></header>
@if(session('success'))<div class="notice success">{{ session('success') }}</div>@endif @if(session('error'))<div class="notice error">{{ session('error') }}</div>@endif @if($errors->any())<div class="notice error">{{ $errors->first() }}</div>@endif
<main>@yield('content')</main><footer><div><strong>CryptoExchange</strong><p>Digital asset exchange infrastructure built for transparent quotes and controlled order processing.</p></div><div><h4>Platform</h4><a href="{{ route('exchange') }}">Exchange</a><a href="{{ route('faq') }}">FAQ</a><a href="{{ route('agreement') }}">Terms</a></div><div><h4>Support</h4><a href="{{ route('contacts') }}">Contact</a><a href="mailto:{{ config('exchange.support_email') }}">{{ config('exchange.support_email') }}</a></div><div><h4>Security</h4><p>HTTPS · rate limits · server-side quotes · audit-ready order events</p></div></footer><div class="copyright">© {{ date('Y') }} CryptoExchange. All rights reserved.</div></body></html>
