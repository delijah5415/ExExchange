@extends('layouts.app',['title'=>'Account — CryptoExchange'])
@section('content')<section class="section"><div class="account-head"><div><div class="eyebrow">ACCOUNT</div><h1>Exchange history</h1><p class="lead">{{ $user['email'] ?? 'Authenticated user' }}</p></div><a class="btn primary" href="{{ route('exchange') }}">New exchange</a></div><div class="panel table-wrap"><table><thead><tr><th>Order</th><th>Pair</th><th>Send</th><th>Receive</th><th>Status</th><th>Created</th></tr></thead><tbody>@forelse($orders as $order)<tr><td><code>{{ substr($order->uuid,0,12) }}…</code></td><td>{{ $order->base->symbol }} → {{ $order->quote->symbol }}</td><td>{{ $order->amount_send }}</td><td>{{ $order->amount_receive }}</td><td><span class="status">{{ str_replace('_',' ',$order->status) }}</span></td><td>{{ $order->created_at->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="6" class="empty">No orders yet.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</section>@endsection
