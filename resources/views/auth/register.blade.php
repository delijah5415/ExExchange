@extends('layouts.app',['title'=>'Create account — CryptoExchange'])
@section('content')<section class="auth"><div class="panel"><div class="eyebrow">ACCOUNT</div><h1>Create account</h1><p>Authentication is handled by Supabase Auth in the production configuration.</p><form method="POST" action="{{ route('register.store') }}">@csrf<div class="field"><label>Email</label><input type="email" name="email" value="{{ old('email') }}" required autocomplete="email"></div><div class="field"><label>Password</label><input type="password" name="password" required minlength="10" autocomplete="new-password"></div><div class="field"><label>Confirm password</label><input type="password" name="password_confirmation" required minlength="10" autocomplete="new-password"></div><button class="btn primary full">Create account</button></form><p class="muted">Already registered? <a href="{{ route('login') }}">Sign in</a>.</p></div></section>@endsection
